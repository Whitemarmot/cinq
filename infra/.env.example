# =============================================================================
# Cinq BTCPay Server - Variables d'environnement obligatoires
# =============================================================================
# COPIER CE FICHIER: cp .env.example .env
# GÉNÉRER LES SECRETS: openssl rand -base64 32
# =============================================================================

# --- Base de données ---
POSTGRES_PASSWORD=   # OBLIGATOIRE: openssl rand -base64 32

# --- Bitcoin Core ---
BITCOIN_NETWORK=mainnet   # mainnet | testnet | regtest
BITCOIN_RPC_USER=btcrpc
BITCOIN_RPC_PASSWORD=     # OBLIGATOIRE: openssl rand -base64 32

# --- Lightning (LND) ---
LND_ALIAS=CinqLightning
LND_EXTERNAL_IP=          # OBLIGATOIRE: IP publique du serveur

# --- BTCPay Server ---
BTCPAY_HOST=              # OBLIGATOIRE: domaine (ex: pay.cinq.example.com)
BTCPAY_PROTOCOL=https

# --- TLS / Let's Encrypt ---
ACME_EMAIL=               # OBLIGATOIRE: email pour certificats TLS
