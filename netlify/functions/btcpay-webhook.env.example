# BTCPay Webhook Configuration
# Copy to .env or set in Netlify dashboard

# BTCPay Server URL (without trailing slash)
BTCPAY_URL=https://btcpay.example.com

# BTCPay API Key (from Store Settings → Access Tokens)
BTCPAY_API_KEY=your-api-key

# BTCPay Store ID (from store URL or settings)
BTCPAY_STORE_ID=your-store-id

# Webhook Secret (create in BTCPay → Store → Webhooks)
# This is used for HMAC signature verification
BTCPAY_WEBHOOK_SECRET=your-webhook-secret

# Supabase (should already be set)
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Gift code salt (should already be set)
GIFT_CODE_SALT=your-unique-salt
