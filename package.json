{
  "name": "cinq",
  "version": "1.0.0",
  "description": "L'anti-réseau social — 5 contacts max, zéro bullshit",
  "main": "index.html",
  "scripts": {
    "dev": "echo 'Dev mode'",
    "build": "echo 'Build OK'",
    "lint": "eslint . --ext .js --ignore-path .gitignore",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "csso-cli": "^4.0.2",
    "terser": "^5.46.0"
  }
}
